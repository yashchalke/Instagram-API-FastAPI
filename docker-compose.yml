services:
  backend:
    build: ./backend
    ports:
      - "8000:8000" 
    env_file:
      - ./Backend/.env


  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    env_file:
      - ./frontend/.env
    depends_on:
      - backend
    